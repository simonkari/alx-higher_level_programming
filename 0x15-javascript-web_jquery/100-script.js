'use strict';
// This code uses the 'load' event listener to runs
window.addEventListener('load', () => {
  // Select the <header> element in the DOM using
  //`document.querySelector`.
  document.querySelector('header').style.color = '#FF0000';
});
